package lang.object.test;

public class Summary {
    // 정리
    // Object의 나머지 메서드
    // clone() : 객체를 복사할 때 사용, 잘 사용하지 않으므로 다루지 않음
    // hashCode() : equals()와 hashCode()는 종종 함께 사용되지만 컬렉션 프레임워크를 이해해야 하므로 뒤에서 자세히 설명한다.
    // getClass() : 뒤에 Class에서 설명한다.
    // notify(), notifyAll(), wait() : 멀티쓰레드용 메서드이므로 멀티쓰레드에서 다룬다.
}
